import tkinter as tk

root = tk.Tk()

root.title("Jarvis AI Assistant")
root.geometry("500x300")

label = tk.Label(
    root,
    text="JARVIS AI ASSISTANT",
    font=("Arial", 20)
)

label.pack(pady=50)

root.mainloop()